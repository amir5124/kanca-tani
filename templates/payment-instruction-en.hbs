<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Payment Instruction</title>
    <style>
        body { 
            font-family: Arial, sans-serif; 
            line-height: 1.6; 
            color: #333; 
            max-width: 600px; 
            margin: 0 auto; 
            padding: 20px; 
        }
        .header { 
            text-align: center; 
            padding: 20px 0; 
            border-bottom: 2px solid #E6F4FE; 
        }
        .logo { 
            font-size: 24px; 
            font-weight: bold; 
            color: #1a5276; 
        }
        .content { padding: 20px 0; }
        .booking-info { 
            background: #f8f9fa; 
            padding: 15px; 
            border-radius: 8px; 
            margin: 15px 0; 
        }
        .info-row { 
            display: flex; 
            justify-content: space-between; 
            padding: 4px 0; 
            border-bottom: 1px solid #eee; 
        }
        .info-row:last-child { border-bottom: none; }
        .label { font-weight: 600; color: #555; }
        .payment-info { 
            background: #E6F4FE; 
            padding: 15px; 
            border-radius: 8px; 
            margin: 15px 0; 
        }
        .total { 
            font-size: 20px; 
            font-weight: bold; 
            color: #1a5276; 
        }
        .footer { 
            text-align: center; 
            padding: 20px 0; 
            border-top: 1px solid #ddd; 
            font-size: 12px; 
            color: #666; 
        }
        .important { 
            color: #e74c3c; 
            font-weight: bold; 
        }
        .badge { 
            display: inline-block; 
            padding: 2px 10px; 
            border-radius: 12px; 
            font-size: 12px; 
            font-weight: 600;
        }
        .badge-pending { background: #fff3cd; color: #856404; }
        .badge-transfer { background: #d1ecf1; color: #0c5460; }
        .badge-fastboat { background: #cce5ff; color: #004085; }
        .badge-ticketboat { background: #d4edda; color: #155724; }
        .service-icon { font-size: 18px; }
    </style>
</head>
<body>
    <div class="header">
        <div class="logo">🚀 Gobali Traveling</div>
        <p>💳 Payment Instruction</p>
    </div>
    
    <div class="content">
        <p>Hello <strong>{{customer_name}}</strong>,</p>
        <p>Thank you for booking with Gobali Traveling. Here are the details of your booking.</p>
        
        <div class="booking-info">
            <h3>📋 Booking Details</h3>
            <div class="info-row">
                <span class="label">Booking Code</span>
                <span><strong>{{booking_reference}}</strong></span>
            </div>
            <div class="info-row">
                <span class="label">Service</span>
                <span>
                    <span class="badge badge-{{service_type}}">
                        {{#if (eq service_type "ticketboat")}}🚢{{/if}}
                        {{#if (eq service_type "fastboat")}}⛴️{{/if}}
                        {{#if (eq service_type "transfer")}}🚗{{/if}}
                        {{service_label}}
                    </span>
                </span>
            </div>
            <div class="info-row">
                <span class="label">Status</span>
                <span><span class="badge badge-pending">Awaiting Payment</span></span>
            </div>

            <!-- ===== TRANSFER ===== -->
            {{#if (eq service_type "transfer")}}
                {{#if pickup_address}}
                <div class="info-row">
                    <span class="label">Pickup</span>
                    <span>{{pickup_address}}</span>
                </div>
                {{/if}}
                {{#if dropoff_address}}
                <div class="info-row">
                    <span class="label">Dropoff</span>
                    <span>{{dropoff_address}}</span>
                </div>
                {{/if}}
                {{#if depart_datetime}}
                <div class="info-row">
                    <span class="label">Departure Date</span>
                    <span>{{depart_datetime}}</span>
                </div>
                {{/if}}
                {{#if return_datetime}}
                <div class="info-row">
                    <span class="label">Return Date</span>
                    <span>{{return_datetime}}</span>
                </div>
                {{/if}}
            {{/if}}

            <!-- ===== FASTBOAT ===== -->
            {{#if (eq service_type "fastboat")}}
                {{#if fb_pickup_port}}
                <div class="info-row">
                    <span class="label">Origin Port</span>
                    <span>{{fb_pickup_port}}</span>
                </div>
                {{/if}}
                {{#if fb_dropoff_port}}
                <div class="info-row">
                    <span class="label">Destination Port</span>
                    <span>{{fb_dropoff_port}}</span>
                </div>
                {{/if}}
                {{#if fb_depart_date}}
                <div class="info-row">
                    <span class="label">Departure Date</span>
                    <span>{{fb_depart_date}}{{#if fb_depart_slot}} ({{fb_depart_slot}}){{/if}}</span>
                </div>
                {{/if}}
                {{#if fb_return_date}}
                <div class="info-row">
                    <span class="label">Return Date</span>
                    <span>{{fb_return_date}}{{#if fb_return_slot}} ({{fb_return_slot}}){{/if}}</span>
                </div>
                {{/if}}
            {{/if}}

            <!-- ===== TICKET BOAT ===== -->
            {{#if (eq service_type "ticketboat")}}
                {{#if tb_pickup_location}}
                <div class="info-row">
                    <span class="label">Origin Port</span>
                    <span>{{tb_pickup_location}}</span>
                </div>
                {{/if}}
                {{#if tb_dropoff_location}}
                <div class="info-row">
                    <span class="label">Destination Port</span>
                    <span>{{tb_dropoff_location}}</span>
                </div>
                {{/if}}
                {{#if tb_ticket_type}}
                <div class="info-row">
                    <span class="label">Ticket Type</span>
                    <span>{{tb_ticket_type}}</span>
                </div>
                {{/if}}
                {{#if tb_depart_date}}
                <div class="info-row">
                    <span class="label">Departure Date</span>
                    <span>{{tb_depart_date}}{{#if tb_depart_time}} ({{tb_depart_time}}){{/if}}</span>
                </div>
                {{/if}}
                {{#if tb_return_date}}
                <div class="info-row">
                    <span class="label">Return Date</span>
                    <span>{{tb_return_date}}{{#if tb_return_time}} ({{tb_return_time}}){{/if}}</span>
                </div>
                {{/if}}
            {{/if}}

            <div class="info-row">
                <span class="label">Passengers</span>
                <span>{{total_pax}} people</span>
            </div>
        </div>
        
        <div class="payment-info">
            <h3>💰 Payment Information</h3>
            <p style="color: #e67e22; font-weight: bold; margin-bottom: 12px;">
                ⚡ Payment is made ONSITE (onsite payment)
            </p>
            <div class="info-row">
                <span class="label">Total Payment</span>
                <span class="total">{{final_price}}</span>
            </div>
            {{#if discount_amount}}
            <div class="info-row">
                <span class="label">Discount</span>
                <span style="color: #27ae60;">- {{discount_amount}}</span>
            </div>
            {{/if}}
            <div class="info-row">
                <span class="label">Payment Method</span>
                <span>💰 Cash / QRIS</span>
            </div>
            <div class="info-row">
                <span class="label">Payment Location</span>
                <span>📍 At the destination / pickup location</span>
            </div>
        </div>
        
        <div style="background: #fff8e1; padding: 15px; border-radius: 8px; border-left: 4px solid #f39c12; margin: 15px 0;">
            <p style="margin: 0; font-size: 14px;">
                <strong>📌 Important Information:</strong><br>
                1. Payment is made <strong>directly onsite</strong> to our driver / crew<br>
                2. Our team will contact you via WhatsApp or Email for confirmation<br>
                3. Please prepare the exact amount to speed up the transaction<br>
                4. Keep your proof of payment for reference
            </p>
        </div>
        
        <div class="important">
            ⚠️ <strong>Attention:</strong> If there is a schedule change or cancellation, please confirm immediately via WhatsApp at <strong>{{app_phone}}</strong>.
        </div>
        
        <p>If you have any questions, please don't hesitate to contact us.</p>
    </div>
    
    <div class="footer">
        <p><strong>{{app_name}}</strong></p>
        <p>📞 {{app_phone}} | ✉️ {{app_email}}</p>
        <p>📍 {{app_address}}</p>
        <p>&copy; {{year}} {{app_name}} - All Rights Reserved</p>
    </div>
</body>
</html>
